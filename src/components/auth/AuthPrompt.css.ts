import { style } from "@vanilla-extract/css";

export const authPromptContainer = style({
  display: "flex",
  flexDirection: "column",
  gap: 16
});

export const authPromptMessage = style({
  marginBottom: "20px"
}); 